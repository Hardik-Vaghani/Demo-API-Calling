<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow access to the external storage pictures directory -->
    <external-path name="external_files" path="." />

    <!-- Optionally, if you want to specifically access the Pictures directory -->
    <external-path name="pictures" path="Pictures/." />

    <!-- For the Downloads directory -->
    <external-path name="downloads" path="Download/." />

    <!-- For the Camera photos directory -->
    <files-path name="camera_photos" path="."/>
</paths>